# CL SDK Runtime Surface

This package contains the vendored CL SDK runtime and simulator interface.

Keep changes here focused on SDK compatibility, simulator plumbing, playback,
application packaging, visualization, and public `cl` API behavior.  Local
experiment code should live in sibling packages such as `cl1_snn_reset` and
`cl1_clsdk_bridge`.
